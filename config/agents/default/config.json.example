# ============================================
# Agent 配置文件
# ============================================
# 保存位置：~/.clawhermes/agents/<name>/config.json
# 运行 clawhermes agent create <name> 自动生成
# ============================================

{
  "model": "deepseek/deepseek-chat",
  "temperature": 0.7,
  "max_iterations": 50
}
